import HeroSlidesManager from "@/components/HeroSlides";

export const metadata = {
  title: "Hero Banner Management | Rajasthalli Admin",
  description: "Configure homepage banner rotation slider links and images.",
};

const HeroSlidesPage = () => {
  return <HeroSlidesManager />;
};

export default HeroSlidesPage;
